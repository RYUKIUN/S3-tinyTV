#pragma once
// This file has been renamed to nexus.h — see that file for the actual
// content (it's now the central, zoned configuration hub for the project).
// This shim exists only so nothing breaks if some include was missed
// during the rename. Safe to delete once you've confirmed the build works
// and nothing references "shared.h" anymore.
#include "nexus.h"
